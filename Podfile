# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MiddleMe' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

	pod 'OpenMultitouchSupport', :git => 'https://github.com/Reeywhaar/OpenMultitouchSupport.git', :branch => 'master'

  # Pods for MiddleMe

  target 'MiddleMeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MiddleMeUITests' do
    # Pods for testing
  end

end
